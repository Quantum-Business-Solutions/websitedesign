node_modules/
.env
.env.local
.DS_Store

# Third-party skills installed via `npx skills add` — reproducible from skills-lock.json,
# not vendored. `.claude/skills/scroll-craft` is a symlink into .agents/.
.agents/
.claude/skills/scroll-craft

node_modules/

__pycache__/
*.pyc

# The gate writes screenshots + report.json here (ignored) but the score trendline is kept.
!verify-out/
verify-out/*
!verify-out/scores.jsonl
